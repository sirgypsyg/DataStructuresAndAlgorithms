#include <iostream>
#include <string>
#include <queue>
#include <vector>
#include <list>
#include <algorithm>


int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    int d, n, m, s;

    std::cin >> d; std::cin.ignore();

    for(int i = 0; i < d ; ++i){
        std::cin >> n >> m >> s; std::cin.ignore();
        for(int j = 0; j < m; ++j){
            
        }
    }
    
   


    return 0;
}