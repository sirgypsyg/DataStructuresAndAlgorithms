#include <iostream>
#include <queue>
#include <vector>

void fillMap(int &arr[][], int m){
    int arr[] = 
}


int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    int n, m, x, y, p;
    std::cin >> n; std::cin.ignore();
    std::cin >> m, x, y, p; std::cin.ignore();

    int map[m][m];
    map[x][y] = 0;
    

    
    return 0;
}